import ContactList from '../components/ContactList'

export default function Leads() {
  return <ContactList segment="lead" title="Leads" subtitle="People to call. Outcomes move them to the right list automatically." allowAdd />
}
